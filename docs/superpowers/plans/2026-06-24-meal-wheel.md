# Meal Wheel Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a mobile-friendly static web app that picks 6 dishes/day (3 meals × protein + carb) from a 1400-dish library, with 4 generation rules, per-dish swap, and copy-to-clipboard. Deployed to GitHub Pages.

**Architecture:** Single-page app, no build step. Pure HTML + CSS + vanilla JS with ES modules. `engine.js` holds the pure generation algorithms (unit tested with Vitest). `app.js` wires the DOM. `sync_dishes.py` converts the user's `.md` recipe files into `data/dishes.json`. PWA-enabled (manifest + service worker) so it installs as a phone home-screen app.

**Tech Stack:** HTML5, CSS3, ES2022 modules, Vitest (dev only), Python 3 (sync script), GitHub Pages (hosting).

**Reference Spec:** `docs/superpowers/specs/2026-06-24-meal-wheel-design.md`

**Working Directory:** `/Users/suunto/recipe-app/`

---

## File Structure

```
/Users/suunto/recipe-app/
├── index.html              # single page
├── app.js                  # DOM wiring, state, event handlers
├── engine.js               # generation algorithms (pure, ESM, unit-tested)
├── formatter.js            # copy-to-clipboard text formatter (pure, ESM, unit-tested)
├── style.css               # warm editorial styles, mobile-first
├── data/
│   └── dishes.json         # generated by sync_dishes.py
├── scripts/
│   └── sync_dishes.py      # .md → JSON converter
├── tests/
│   ├── engine.test.js
│   └── formatter.test.js
├── icons/
│   ├── icon-192.png        # PWA icon
│   └── icon-512.png        # PWA icon
├── manifest.webmanifest    # PWA manifest
├── sw.js                   # service worker (cache-first)
├── package.json            # vitest dev dep only
├── vitest.config.js
├── .gitignore              # already created
└── README.md
```

**Decomposition rationale:** `engine.js` and `formatter.js` are pure functions (no DOM) so they can be unit-tested without a browser. `app.js` is the only file that touches the DOM and stays small (~100 lines).

---

## Task 1: Verify project setup and install Vitest

**Files:**
- Create: `package.json`, `vitest.config.js`
- Modify: (none)

- [ ] **Step 1: Verify `.gitignore` exists**

Run: `cat /Users/suunto/recipe-app/.gitignore`
Expected output contains `node_modules/`, `.superpowers/`, `*.log`.

- [ ] **Step 2: Create `package.json`**

Write to `/Users/suunto/recipe-app/package.json`:

```json
{
  "name": "meal-wheel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 3: Install dependencies**

Run: `cd /Users/suunto/recipe-app && npm install`
Expected: `node_modules/` and `package-lock.json` created. No errors.

- [ ] **Step 4: Create `vitest.config.js`**

Write to `/Users/suunto/recipe-app/vitest.config.js`:

```js
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['tests/**/*.test.js'],
    globals: false,
  },
});
```

- [ ] **Step 5: Write a trivial test to verify the runner**

Write to `/Users/suunto/recipe-app/tests/sanity.test.js`:

```js
import { describe, it, expect } from 'vitest';

describe('sanity', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
```

- [ ] **Step 6: Run the test, verify it passes**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: 1 test passed.

- [ ] **Step 7: Commit**

```bash
cd /Users/suunto/recipe-app
git add package.json package-lock.json vitest.config.js tests/sanity.test.js
git commit -m "chore: add vitest test runner"
```

---

## Task 2: Build `sync_dishes.py` to convert .md files to JSON

**Files:**
- Create: `scripts/sync_dishes.py`
- Test: run on real data, verify output

- [ ] **Step 1: Create `scripts/` directory**

Run: `mkdir -p /Users/suunto/recipe-app/scripts`

- [ ] **Step 2: Write `sync_dishes.py`**

Write to `/Users/suunto/recipe-app/scripts/sync_dishes.py`:

```python
#!/usr/bin/env python3
"""Convert recipe .md files to data/dishes.json.

Source: /Users/suunto/Documents/饮食菜品库/  (8 files, one per category)
Output: /Users/suunto/recipe-app/data/dishes.json

Each .md file's first non-empty line is the category header; remaining lines are
dish names. For this dataset, the file name itself encodes the category, so we
ignore the in-file header and use the file name.
"""
import json
import sys
from pathlib import Path

SOURCE_DIR = Path("/Users/suunto/Documents/饮食菜品库")
OUTPUT_FILE = Path(__file__).resolve().parent.parent / "data" / "dishes.json"

# Mapping: source filename (without .md) -> output key in dishes.json
# Both happen to be identical for this dataset, but we keep the mapping for
# future-proofing.
CATEGORY_MAP = {
    "中式早餐蛋白": "中式早餐蛋白",
    "中式早餐碳水": "中式早餐碳水",
    "中式正餐蛋白": "中式正餐蛋白",
    "中式正餐碳水": "中式正餐碳水",
    "西式早餐蛋白": "西式早餐蛋白",
    "西式早餐碳水": "西式早餐碳水",
    "西式正餐蛋白": "西式正餐蛋白",
    "西式正餐碳水": "西式正餐碳水",
}


def parse_md(path: Path) -> list[str]:
    """Return deduplicated, stripped, non-empty lines from a .md file."""
    text = path.read_text(encoding="utf-8")
    seen = set()
    result = []
    for line in text.splitlines():
        s = line.strip()
        if not s or s.startswith("#"):
            continue
        if s in seen:
            continue
        seen.add(s)
        result.append(s)
    return result


def main() -> int:
    if not SOURCE_DIR.is_dir():
        print(f"ERROR: source dir not found: {SOURCE_DIR}", file=sys.stderr)
        return 1

    categories: dict[str, list[str]] = {}
    for src_name, out_key in CATEGORY_MAP.items():
        src_path = SOURCE_DIR / f"{src_name}.md"
        if not src_path.is_file():
            print(f"ERROR: missing source file: {src_path}", file=sys.stderr)
            return 1
        categories[out_key] = parse_md(src_path)

    OUTPUT_FILE.parent.mkdir(parents=True, exist_ok=True)
    output = {"version": 1, "categories": categories}
    OUTPUT_FILE.write_text(
        json.dumps(output, ensure_ascii=False, indent=2) + "\n",
        encoding="utf-8",
    )

    total = sum(len(v) for v in categories.values())
    print(f"OK: {len(categories)} categories, {total} dishes total")
    for k, v in categories.items():
        print(f"  {k}: {len(v)}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 3: Run the script**

Run: `cd /Users/suunto/recipe-app && python3 scripts/sync_dishes.py`
Expected output:
```
OK: 8 categories, 1396 dishes total
  中式早餐蛋白: 125
  中式早餐碳水: 133
  中式正餐蛋白: 446
  中式正餐碳水: 199
  西式早餐蛋白: 123
  西式早餐碳水: 120
  西式正餐蛋白: 163
  西式正餐碳水: 87
```
(Totals may vary slightly based on the user's library.)

- [ ] **Step 4: Verify the JSON is valid and has expected structure**

Run: `cd /Users/suunto/recipe-app && python3 -c "import json; d=json.load(open('data/dishes.json')); assert d['version']==1; assert len(d['categories'])==8; print('OK')"`
Expected: `OK`

- [ ] **Step 5: Spot-check a category**

Run: `cd /Users/suunto/recipe-app && python3 -c "import json; d=json.load(open('data/dishes.json')); print(d['categories']['中式早餐蛋白'][:5])"`
Expected: `['水煮蛋', '茶叶蛋', '温泉蛋', '溏心蛋', '荷包蛋']` (or similar — first 5 dishes of that category).

- [ ] **Step 6: Commit**

```bash
cd /Users/suunto/recipe-app
git add scripts/sync_dishes.py data/dishes.json
git commit -m "feat(data): sync_dishes.py generates data/dishes.json from .md library"
```

---

## Task 3: TDD `engine.js` — `categoryKeyFor` helper

**Files:**
- Create: `src/engine.js`, `tests/engine.test.js`
- (All `src/` and `tests/` files live at repo root in this project; the `src/` prefix here is conceptual. The actual path is `/Users/suunto/recipe-app/engine.js` and `/Users/suunto/recipe-app/tests/engine.test.js`.)

**Path note:** To keep the browser load simple (no `src/` prefix in `index.html`), all JS files live at the repo root. Test imports use `../engine.js`.

- [ ] **Step 1: Create `tests/` directory**

Run: `mkdir -p /Users/suunto/recipe-app/tests`

- [ ] **Step 2: Write the failing test for `categoryKeyFor`**

Write to `/Users/suunto/recipe-app/tests/engine.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { categoryKeyFor } from '../engine.js';

describe('categoryKeyFor', () => {
  it('maps breakfast protein to 中式早餐蛋白', () => {
    expect(categoryKeyFor({ meal: 'breakfast', kind: 'protein' }, '中式'))
      .toBe('中式早餐蛋白');
  });

  it('maps breakfast carb to 西式早餐碳水', () => {
    expect(categoryKeyFor({ meal: 'breakfast', kind: 'carb' }, '西式'))
      .toBe('西式早餐碳水');
  });

  it('maps lunch protein to 中式正餐蛋白', () => {
    expect(categoryKeyFor({ meal: 'lunch', kind: 'protein' }, '中式'))
      .toBe('中式正餐蛋白');
  });

  it('maps dinner carb to 西式正餐碳水', () => {
    expect(categoryKeyFor({ meal: 'dinner', kind: 'carb' }, '西式'))
      .toBe('西式正餐碳水');
  });
});
```

- [ ] **Step 3: Run the test, verify it fails**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: FAIL with "categoryKeyFor is not a function" or similar.

- [ ] **Step 4: Create `engine.js` with the `categoryKeyFor` implementation**

Write to `/Users/suunto/recipe-app/engine.js`:

```js
// engine.js — pure generation algorithms for Meal Wheel.
// No DOM access. No side effects. Unit-tested via tests/engine.test.js.

/**
 * @typedef {'breakfast' | 'lunch' | 'dinner'} Meal
 * @typedef {'protein' | 'carb'} Kind
 * @typedef {'中式' | '西式'} Flavor
 * @typedef {{ meal: Meal, kind: Kind }} Slot
 */

/** 6 slots that make up a daily plan, in display order. */
export const SLOTS = [
  { id: 'breakfast-protein', meal: 'breakfast', kind: 'protein' },
  { id: 'breakfast-carb',    meal: 'breakfast', kind: 'carb' },
  { id: 'lunch-protein',     meal: 'lunch',     kind: 'protein' },
  { id: 'lunch-carb',        meal: 'lunch',     kind: 'carb' },
  { id: 'dinner-protein',    meal: 'dinner',    kind: 'protein' },
  { id: 'dinner-carb',       meal: 'dinner',    kind: 'carb' },
];

/**
 * Resolve the category key in dishes.json for a given slot + flavor.
 * Example: ({meal:'breakfast', kind:'protein'}, '中式') -> '中式早餐蛋白'
 */
export function categoryKeyFor(slot, flavor) {
  const meal = { breakfast: '早餐', lunch: '正餐', dinner: '正餐' }[slot.meal];
  const kind = slot.kind === 'protein' ? '蛋白' : '碳水';
  return `${flavor}${meal}${kind}`;
}
```

- [ ] **Step 5: Run the test, verify it passes**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: all 4 `categoryKeyFor` tests pass.

- [ ] **Step 6: Commit**

```bash
cd /Users/suunto/recipe-app
git add engine.js tests/engine.test.js
git commit -m "feat(engine): add categoryKeyFor with categoryKeyFor tests"
```

---

## Task 4: TDD `engine.js` — `noRepeat` rule

**Files:**
- Modify: `engine.js`, `tests/engine.test.js`

- [ ] **Step 1: Add failing tests for `noRepeat`**

Append to `/Users/suunto/recipe-app/tests/engine.test.js`:

```js
import { noRepeat } from '../engine.js';

describe('noRepeat', () => {
  // Helper: a tiny in-memory library.
  const categories = {
    '中式早餐蛋白': ['蛋A', '蛋B', '蛋C'],
    '中式早餐碳水': ['包A', '包B', '包C'],
    '中式正餐蛋白': ['鱼A', '鱼B', '鱼C'],
    '中式正餐碳水': ['饭A', '饭B', '饭C'],
    '西式早餐蛋白': ['西A', '西B', '西C'],
    '西式早餐碳水': ['西D', '西E', '西F'],
    '西式正餐蛋白': ['西G', '西H', '西I'],
    '西式正餐碳水': ['西J', '西K', '西L'],
  };

  it('returns 6 dishes, one per slot', () => {
    const result = noRepeat(categories);
    expect(result).toHaveLength(6);
    expect(result[0]).toHaveProperty('slotId');
    expect(result[0]).toHaveProperty('dish');
    expect(result[0]).toHaveProperty('flavor');
  });

  it('produces all unique dishes (no repeat)', () => {
    // Run many times; the result must always have 6 distinct dishes.
    for (let i = 0; i < 20; i++) {
      const result = noRepeat(categories);
      const dishes = result.map(r => r.dish);
      expect(new Set(dishes).size).toBe(6);
    }
  });

  it('falls back to allowing repeats when a category is exhausted', () => {
    // 1 dish per category is not enough to satisfy no-repeat for all 6 slots
    // sharing a flavor.
    const tiny = {
      '中式早餐蛋白': ['蛋A'],
      '中式早餐碳水': ['包A'],
      '中式正餐蛋白': ['鱼A'],
      '中式正餐碳水': ['饭A'],
      '西式早餐蛋白': ['西A'],
      '西式早餐碳水': ['西D'],
      '西式正餐蛋白': ['西G'],
      '西式正餐碳水': ['西J'],
    };
    // Should not throw; should return 6 entries.
    const result = noRepeat(tiny);
    expect(result).toHaveLength(6);
  });
});
```

- [ ] **Step 2: Run, verify tests fail (noRepeat undefined)**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: 2 new failures ("noRepeat is not a function").

- [ ] **Step 3: Implement `noRepeat` in `engine.js`**

Append to `/Users/suunto/recipe-app/engine.js`:

```js
/**
 * Pick a random element from an array.
 * @template T
 * @param {T[]} arr
 * @returns {T}
 */
function pickOne(arr) {
  return arr[Math.floor(Math.random() * arr.length)];
}

/**
 * Decide the flavor for each meal. The `noRepeat` rule can mix flavors
 * freely (it doesn't constrain flavors), so this is the same as `pureRandom`.
 * Exported so other rules can share it.
 * @returns {{breakfast: Flavor, lunch: Flavor, dinner: Flavor}}
 */
function pickFlavorsFree() {
  return {
    breakfast: Math.random() < 0.5 ? '中式' : '西式',
    lunch:     Math.random() < 0.5 ? '中式' : '西式',
    dinner:    Math.random() < 0.5 ? '中式' : '西式',
  };
}

/**
 * Rule 1: noRepeat — all 6 dishes are unique within the day.
 * @param {Record<string, string[]>} categories — the dish library.
 * @returns {Array<{slotId: string, dish: string, flavor: Flavor}>}
 */
export function noRepeat(categories) {
  const used = new Set();
  const flavors = pickFlavorsFree();
  return SLOTS.map(slot => {
    const key = categoryKeyFor(slot, flavors[slot.meal]);
    const pool = categories[key].filter(d => !used.has(d));
    const pick = pool.length ? pickOne(pool) : pickOne(categories[key]);
    used.add(pick);
    return { slotId: slot.id, dish: pick, flavor: flavors[slot.meal] };
  });
}
```

- [ ] **Step 4: Run, verify tests pass**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: all `noRepeat` tests pass; `categoryKeyFor` tests still pass.

- [ ] **Step 5: Commit**

```bash
cd /Users/suunto/recipe-app
git add engine.js tests/engine.test.js
git commit -m "feat(engine): noRepeat rule"
```

---

## Task 5: TDD `engine.js` — `pureRandom` rule

**Files:** Modify `engine.js`, `tests/engine.test.js`

- [ ] **Step 1: Add failing test for `pureRandom`**

Append to `/Users/suunto/recipe-app/tests/engine.test.js`:

```js
import { pureRandom } from '../engine.js';

describe('pureRandom', () => {
  const categories = {
    '中式早餐蛋白': ['蛋A', '蛋B', '蛋C'],
    '中式早餐碳水': ['包A', '包B', '包C'],
    '中式正餐蛋白': ['鱼A', '鱼B', '鱼C'],
    '中式正餐碳水': ['饭A', '饭B', '饭C'],
    '西式早餐蛋白': ['西A', '西B', '西C'],
    '西式早餐碳水': ['西D', '西E', '西F'],
    '西式正餐蛋白': ['西G', '西H', '西I'],
    '西式正餐碳水': ['西J', '西K', '西L'],
  };

  it('returns 6 dishes', () => {
    expect(pureRandom(categories)).toHaveLength(6);
  });

  it('can produce the same dish twice across 6 slots (repeats allowed)', () => {
    // With a 1-dish library per category, all 6 picks must be that dish.
    const tiny = Object.fromEntries(
      Object.entries(categories).map(([k, v]) => [k, [v[0]]])
    );
    const dishes = pureRandom(tiny).map(r => r.dish);
    expect(new Set(dishes).size).toBe(1);
  });
});
```

- [ ] **Step 2: Run, verify fail**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: failure on `pureRandom is not a function`.

- [ ] **Step 3: Implement `pureRandom`**

Append to `/Users/suunto/recipe-app/engine.js`:

```js
/**
 * Rule 2: pureRandom — each slot picks independently; repeats allowed.
 */
export function pureRandom(categories) {
  const flavors = pickFlavorsFree();
  return SLOTS.map(slot => {
    const key = categoryKeyFor(slot, flavors[slot.meal]);
    return { slotId: slot.id, dish: pickOne(categories[key]), flavor: flavors[slot.meal] };
  });
}
```

- [ ] **Step 4: Run, verify pass**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: all tests pass.

- [ ] **Step 5: Commit**

```bash
cd /Users/suunto/recipe-app
git add engine.js tests/engine.test.js
git commit -m "feat(engine): pureRandom rule"
```

---

## Task 6: TDD `engine.js` — `flavorRotation` rule

**Files:** Modify `engine.js`, `tests/engine.test.js`

- [ ] **Step 1: Add failing test for `flavorRotation`**

Append to `/Users/suunto/recipe-app/tests/engine.test.js`:

```js
import { flavorRotation } from '../engine.js';

describe('flavorRotation', () => {
  const categories = {
    '中式早餐蛋白': ['中早蛋A', '中早蛋B'],
    '中式早餐碳水': ['中早包A', '中早包B'],
    '中式正餐蛋白': ['中正鱼A', '中正鱼B'],
    '中式正餐碳水': ['中正饭A', '中正饭B'],
    '西式早餐蛋白': ['西早蛋A', '西早蛋B'],
    '西式早餐碳水': ['西早包A', '西早包B'],
    '西式正餐蛋白': ['西正鱼A', '西正鱼B'],
    '西式正餐碳水': ['西正饭A', '西正饭B'],
  };

  it('returns 6 dishes', () => {
    expect(flavorRotation(categories)).toHaveLength(6);
  });

  it('breakfast flavor == lunch flavor, dinner flavor is the other', () => {
    for (let i = 0; i < 20; i++) {
      const result = flavorRotation(categories);
      const byMeal = Object.fromEntries(
        ['breakfast', 'lunch', 'dinner'].map(m => [
          m,
          result.filter(r => r.slotId.startsWith(m))[0].flavor,
        ])
      );
      expect(byMeal.breakfast).toBe(byMeal.lunch);
      expect(byMeal.dinner).not.toBe(byMeal.breakfast);
    }
  });

  it('picks from the right category for the assigned flavor', () => {
    const result = flavorRotation(categories);
    for (const r of result) {
      const slot = SLOTS.find(s => s.id === r.slotId);
      const key = categoryKeyFor(slot, r.flavor);
      expect(categories[key]).toContain(r.dish);
    }
  });
});
```

- [ ] **Step 2: Run, verify fail**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: failure on `flavorRotation is not a function`.

- [ ] **Step 3: Implement `flavorRotation`**

Append to `/Users/suunto/recipe-app/engine.js`:

```js
/**
 * Rule 3: flavorRotation — breakfast + lunch share one flavor; dinner uses the other.
 */
export function flavorRotation(categories) {
  const breakfastFlavor = Math.random() < 0.5 ? '中式' : '西式';
  const dinnerFlavor = breakfastFlavor === '中式' ? '西式' : '中式';
  const flavors = {
    breakfast: breakfastFlavor,
    lunch:     breakfastFlavor,  // lunch shares with breakfast
    dinner:    dinnerFlavor,
  };
  return SLOTS.map(slot => {
    const key = categoryKeyFor(slot, flavors[slot.meal]);
    return { slotId: slot.id, dish: pickOne(categories[key]), flavor: flavors[slot.meal] };
  });
}
```

- [ ] **Step 4: Run, verify pass**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: all tests pass.

- [ ] **Step 5: Commit**

```bash
cd /Users/suunto/recipe-app
git add engine.js tests/engine.test.js
git commit -m "feat(engine): flavorRotation rule"
```

---

## Task 7: TDD `engine.js` — `mainRepeat` rule

**Files:** Modify `engine.js`, `tests/engine.test.js`

- [ ] **Step 1: Add failing test for `mainRepeat`**

Append to `/Users/suunto/recipe-app/tests/engine.test.js`:

```js
import { mainRepeat } from '../engine.js';

describe('mainRepeat', () => {
  const categories = {
    '中式早餐蛋白': ['早蛋A', '早蛋B'],
    '中式早餐碳水': ['早包A', '早包B'],
    '中式正餐蛋白': ['中鱼A', '中鱼B', '中鱼C'],
    '中式正餐碳水': ['中饭A', '中饭B', '中饭C'],
    '西式早餐蛋白': ['西早A', '西早B'],
    '西式早餐碳水': ['西早C', '西早D'],
    '西式正餐蛋白': ['西正A', '西正B', '西正C'],
    '西式正餐碳水': ['西正D', '西正E', '西正F'],
  };

  it('returns 6 dishes', () => {
    expect(mainRepeat(categories)).toHaveLength(6);
  });

  it('lunch protein and dinner protein are the same dish', () => {
    for (let i = 0; i < 20; i++) {
      const result = mainRepeat(categories);
      const lunchP = result.find(r => r.slotId === 'lunch-protein').dish;
      const dinnerP = result.find(r => r.slotId === 'dinner-protein').dish;
      expect(lunchP).toBe(dinnerP);
    }
  });

  it('lunch carb and dinner carb are different dishes (when possible)', () => {
    for (let i = 0; i < 20; i++) {
      const result = mainRepeat(categories);
      const lunchC = result.find(r => r.slotId === 'lunch-carb').dish;
      const dinnerC = result.find(r => r.slotId === 'dinner-carb').dish;
      // If both meals share a flavor and that category has ≥ 2 dishes,
      // carbs will differ. The test library has 3 in each; we just check
      // the structural property that the swap is valid.
      expect(lunchC).toBeTruthy();
      expect(dinnerC).toBeTruthy();
    }
  });
});
```

- [ ] **Step 2: Run, verify fail**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: failure on `mainRepeat is not a function`.

- [ ] **Step 3: Implement `mainRepeat`**

Append to `/Users/suunto/recipe-app/engine.js`:

```js
/**
 * Rule 4: mainRepeat — lunch protein = dinner protein (cook once, eat twice).
 * Carbs for lunch and dinner differ. Breakfast is independent.
 */
export function mainRepeat(categories) {
  const breakfastFlavor = Math.random() < 0.5 ? '中式' : '西式';
  const mainFlavor = Math.random() < 0.5 ? '中式' : '西式';
  const flavors = {
    breakfast: breakfastFlavor,
    lunch:     mainFlavor,
    dinner:    mainFlavor,
  };

  // Lunch protein = Dinner protein (shared).
  const mainProteinKey = categoryKeyFor(
    { meal: 'lunch', kind: 'protein' },
    mainFlavor
  );
  const sharedProtein = pickOne(categories[mainProteinKey]);

  // Lunch carb: pick from mainFlavor's main-carb category.
  const lunchCarbKey = categoryKeyFor(
    { meal: 'lunch', kind: 'carb' },
    mainFlavor
  );
  const lunchCarb = pickOne(categories[lunchCarbKey]);

  // Dinner carb: different from lunch carb, within same category.
  const dinnerCarbKey = categoryKeyFor(
    { meal: 'dinner', kind: 'carb' },
    mainFlavor
  );
  const carbPool = categories[dinnerCarbKey].filter(d => d !== lunchCarb);
  const dinnerCarb = carbPool.length
    ? pickOne(carbPool)
    : pickOne(categories[dinnerCarbKey]);

  // Breakfast: independent picks.
  const breakfastProteinKey = categoryKeyFor(
    { meal: 'breakfast', kind: 'protein' },
    breakfastFlavor
  );
  const breakfastCarbKey = categoryKeyFor(
    { meal: 'breakfast', kind: 'carb' },
    breakfastFlavor
  );

  return [
    { slotId: 'breakfast-protein', dish: pickOne(categories[breakfastProteinKey]), flavor: breakfastFlavor },
    { slotId: 'breakfast-carb',    dish: pickOne(categories[breakfastCarbKey]),    flavor: breakfastFlavor },
    { slotId: 'lunch-protein',     dish: sharedProtein,                            flavor: mainFlavor },
    { slotId: 'lunch-carb',        dish: lunchCarb,                                flavor: mainFlavor },
    { slotId: 'dinner-protein',    dish: sharedProtein,                            flavor: mainFlavor },
    { slotId: 'dinner-carb',       dish: dinnerCarb,                               flavor: mainFlavor },
  ];
}
```

- [ ] **Step 4: Run, verify pass**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: all tests pass.

- [ ] **Step 5: Commit**

```bash
cd /Users/suunto/recipe-app
git add engine.js tests/engine.test.js
git commit -m "feat(engine): mainRepeat rule"
```

---

## Task 8: TDD `engine.js` — `swapDish` function

**Files:** Modify `engine.js`, `tests/engine.test.js`

- [ ] **Step 1: Add failing test for `swapDish`**

Append to `/Users/suunto/recipe-app/tests/engine.test.js`:

```js
import { swapDish } from '../engine.js';

describe('swapDish', () => {
  const categories = {
    '中式早餐蛋白': ['蛋A', '蛋B', '蛋C'],
    '中式早餐碳水': ['包A', '包B', '包C'],
    '中式正餐蛋白': ['鱼A', '鱼B', '鱼C'],
    '中式正餐碳水': ['饭A', '饭B', '饭C'],
    '西式早餐蛋白': ['西A', '西B', '西C'],
    '西式早餐碳水': ['西D', '西E', '西F'],
    '西式正餐蛋白': ['西G', '西H', '西I'],
    '西式正餐碳水': ['西J', '西K', '西L'],
  };

  it('returns a new dish from the same category, different from current', () => {
    const current = [
      { slotId: 'breakfast-protein', dish: '蛋A', flavor: '中式' },
      { slotId: 'lunch-protein',     dish: '鱼A', flavor: '中式' },
    ];
    for (let i = 0; i < 20; i++) {
      const next = swapDish(current, 'breakfast-protein', categories, []);
      const slot = SLOTS.find(s => s.id === 'breakfast-protein');
      const key = categoryKeyFor(slot, '中式');
      expect(categories[key]).toContain(next.dish);
      expect(next.dish).not.toBe('蛋A');
    }
  });

  it('respects the no-repeat constraint when a list of "used" dishes is given', () => {
    const current = [
      { slotId: 'breakfast-protein', dish: '蛋A', flavor: '中式' },
      { slotId: 'breakfast-carb',    dish: '包A', flavor: '中式' },
      { slotId: 'lunch-protein',     dish: '鱼A', flavor: '中式' },
      { slotId: 'lunch-carb',        dish: '饭A', flavor: '中式' },
      { slotId: 'dinner-protein',    dish: '西G', flavor: '西式' },
      { slotId: 'dinner-carb',       dish: '西J', flavor: '西式' },
    ];
    const used = current.map(c => c.dish);
    // Swap breakfast-protein. The new dish must be in the 中式早餐蛋白 category
    // and not in `used`. '蛋B' and '蛋C' qualify.
    for (let i = 0; i < 20; i++) {
      const next = swapDish(current, 'breakfast-protein', categories, used);
      expect(['蛋B', '蛋C']).toContain(next.dish);
    }
  });

  it('falls back to allowing the current dish if the category is exhausted', () => {
    // Only one dish in 中式早餐蛋白; swapping must still return that dish.
    const tiny = { ...categories, '中式早餐蛋白': ['蛋A'] };
    const current = [
      { slotId: 'breakfast-protein', dish: '蛋A', flavor: '中式' },
    ];
    const next = swapDish(current, 'breakfast-protein', tiny, ['蛋A']);
    expect(next.dish).toBe('蛋A');
  });
});
```

- [ ] **Step 2: Run, verify fail**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: failure on `swapDish is not a function`.

- [ ] **Step 3: Implement `swapDish`**

Append to `/Users/suunto/recipe-app/engine.js`:

```js
/**
 * Swap a single dish on the plate.
 * @param {Array<{slotId: string, dish: string, flavor: Flavor}>} current
 * @param {string} slotId — the slot to swap
 * @param {Record<string, string[]>} categories
 * @param {string[]} used — dishes already on the plate (for no-repeat)
 * @returns {{slotId: string, dish: string, flavor: Flavor}}
 */
export function swapDish(current, slotId, categories, used = []) {
  const target = current.find(c => c.slotId === slotId);
  const slot = SLOTS.find(s => s.id === slotId);
  const key = categoryKeyFor(slot, target.flavor);
  const pool = categories[key].filter(d => d !== target.dish && !used.includes(d));
  // If the only available dish is the current one (exhausted category or all
  // others already used), fall back to any dish in the category except the
  // current — and if that's empty too, allow the current.
  if (pool.length) return { slotId, dish: pickOne(pool), flavor: target.flavor };
  const altPool = categories[key].filter(d => d !== target.dish);
  if (altPool.length) return { slotId, dish: pickOne(altPool), flavor: target.flavor };
  return { slotId, dish: target.dish, flavor: target.flavor };
}
```

- [ ] **Step 4: Run, verify pass**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: all tests pass.

- [ ] **Step 5: Commit**

```bash
cd /Users/suunto/recipe-app
git add engine.js tests/engine.test.js
git commit -m "feat(engine): swapDish"
```

---

## Task 9: TDD `formatter.js` — `formatCopyText`

**Files:** Create `formatter.js`, `tests/formatter.test.js`

- [ ] **Step 1: Add failing test**

Write to `/Users/suunto/recipe-app/tests/formatter.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { formatCopyText } from '../formatter.js';

describe('formatCopyText', () => {
  it('formats a plan into plain text', () => {
    const plan = [
      { slotId: 'breakfast-protein', dish: '茶叶蛋', flavor: '中式' },
      { slotId: 'breakfast-carb',    dish: '全麦吐司', flavor: '中式' },
      { slotId: 'lunch-protein',     dish: '清蒸鲈鱼', flavor: '中式' },
      { slotId: 'lunch-carb',        dish: '杂粮饭', flavor: '中式' },
      { slotId: 'dinner-protein',    dish: '白切鸡', flavor: '中式' },
      { slotId: 'dinner-carb',       dish: '燕麦粥', flavor: '中式' },
    ];
    const text = formatCopyText(plan, new Date('2026-06-24T00:00:00'));
    expect(text).toBe(`2026/06/24 今日食谱
🌅 早餐
  · 蛋白：茶叶蛋
  · 碳水：全麦吐司
☀️ 午餐
  · 蛋白：清蒸鲈鱼
  · 碳水：杂粮饭
🌙 晚餐
  · 蛋白：白切鸡
  · 碳水：燕麦粥`);
  });

  it('pads single-digit month and day with zeros', () => {
    const plan = [
      { slotId: 'breakfast-protein', dish: 'A', flavor: '中式' },
      { slotId: 'breakfast-carb',    dish: 'A', flavor: '中式' },
      { slotId: 'lunch-protein',     dish: 'A', flavor: '中式' },
      { slotId: 'lunch-carb',        dish: 'A', flavor: '中式' },
      { slotId: 'dinner-protein',    dish: 'A', flavor: '中式' },
      { slotId: 'dinner-carb',       dish: 'A', flavor: '中式' },
    ];
    const text = formatCopyText(plan, new Date('2026-01-05T00:00:00'));
    expect(text.startsWith('2026/01/05 今日食谱\n')).toBe(true);
  });
});
```

- [ ] **Step 2: Run, verify fail**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: failure on `formatCopyText is not a function`.

- [ ] **Step 3: Implement `formatCopyText`**

Write to `/Users/suunto/recipe-app/formatter.js`:

```js
// formatter.js — pure functions for displaying/copying a plan.

const MEAL_LABELS = {
  breakfast: '🌅 早餐',
  lunch:     '☀️ 午餐',
  dinner:    '🌙 晚餐',
};

const KIND_LABELS = {
  protein: '蛋白',
  carb:    '碳水',
};

function pad2(n) {
  return n < 10 ? `0${n}` : String(n);
}

function formatDate(d) {
  return `${d.getFullYear()}/${pad2(d.getMonth() + 1)}/${pad2(d.getDate())}`;
}

/**
 * Format a plan as plain text suitable for clipboard.
 * @param {Array<{slotId: string, dish: string, flavor: '中式'|'西式'}>} plan
 * @param {Date} date
 * @returns {string}
 */
export function formatCopyText(plan, date) {
  const lines = [`${formatDate(date)} 今日食谱`];
  const meals = ['breakfast', 'lunch', 'dinner'];
  for (const meal of meals) {
    lines.push(MEAL_LABELS[meal]);
    for (const kind of ['protein', 'carb']) {
      const slotId = `${meal}-${kind}`;
      const entry = plan.find(p => p.slotId === slotId);
      lines.push(`  · ${KIND_LABELS[kind]}：${entry.dish}`);
    }
  }
  return lines.join('\n');
}
```

- [ ] **Step 4: Run, verify pass**

Run: `cd /Users/suunto/recipe-app && npm test`
Expected: all tests pass.

- [ ] **Step 5: Commit**

```bash
cd /Users/suunto/recipe-app
git add formatter.js tests/formatter.test.js
git commit -m "feat(formatter): formatCopyText"
```

---

## Task 10: Build `index.html` skeleton

**Files:** Create `index.html`

- [ ] **Step 1: Write `index.html`**

Write to `/Users/suunto/recipe-app/index.html`:

```html
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#fdf6e3">
  <title>温润的餐桌</title>
  <link rel="manifest" href="manifest.webmanifest">
  <link rel="stylesheet" href="style.css">
  <link rel="icon" href="icons/icon-192.png" type="image/png">
</head>
<body>
  <main id="app">
    <header class="hero">
      <div class="hero-date" id="hero-date">— · — · —</div>
      <h1 class="hero-title">温润的餐桌</h1>
      <p class="hero-sub">— 为你今日配的六道菜 —</p>
    </header>

    <div class="rule-bar">
      <label for="rule-select" class="rule-label">规则</label>
      <select id="rule-select" class="rule-select">
        <option value="noRepeat">一天不重复</option>
        <option value="pureRandom">纯随机</option>
        <option value="flavorRotation">风味轮换</option>
        <option value="mainRepeat">主菜重复副菜换</option>
      </select>
    </div>

    <section id="meals" class="meals"></section>

    <div class="actions">
      <button id="copy-btn"  class="btn btn-secondary" type="button">📋 复制今日</button>
      <button id="reroll-btn" class="btn btn-primary"   type="button">🎲 重新配餐</button>
    </div>

    <p class="tip">tip: 不喜欢某道？点旁边的"换一道"</p>

    <div id="error" class="error" hidden></div>
  </main>

  <script type="module" src="app.js"></script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
cd /Users/suunto/recipe-app
git add index.html
git commit -m "feat(ui): index.html skeleton"
```

---

## Task 11: Build `style.css` — tokens + typography

**Files:** Create `style.css`

- [ ] **Step 1: Write `style.css` (full file)**

Write to `/Users/suunto/recipe-app/style.css`:

```css
/* Meal Wheel — warm editorial styles, mobile-first */

:root {
  --bg:           #fdf6e3;
  --surface:      rgba(255, 255, 255, 0.55);
  --border:       #e7d6b3;
  --border-dash:  rgba(180, 83, 9, 0.15);
  --text:         #451a03;
  --text-muted:   #92400e;
  --accent:       #b45309;
  --text-cta:     #fdf6e3;

  --serif:        Georgia, "Times New Roman", "Songti SC", "Noto Serif CJK SC", serif;
  --sans:         -apple-system, BlinkMacSystemFont, "Helvetica Neue", "PingFang SC", "Noto Sans CJK SC", sans-serif;

  --radius-card:  14px;
  --radius-bar:   12px;
  --radius-pill:  999px;

  --space-1:      6px;
  --space-2:      10px;
  --space-3:      14px;
  --space-4:      20px;
}

* { box-sizing: border-box; }

html, body {
  margin: 0;
  padding: 0;
  background: var(--bg);
  color: var(--text);
  font-family: var(--sans);
  font-size: 15px;
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
  -webkit-text-size-adjust: 100%;
}

#app {
  max-width: 420px;
  margin: 0 auto;
  padding: 20px 18px 32px;
}

.hero {
  text-align: left;
  margin-bottom: var(--space-4);
}

.hero-date {
  font-family: var(--sans);
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--accent);
  text-transform: uppercase;
  margin-bottom: 4px;
}

.hero-title {
  font-family: var(--serif);
  font-size: 32px;
  font-weight: 400;
  font-style: italic;
  line-height: 1.1;
  margin: 0 0 4px;
  letter-spacing: 0.5px;
}

.hero-sub {
  font-family: var(--serif);
  font-size: 12px;
  color: var(--text-muted);
  font-style: italic;
  margin: 0;
}

.rule-bar {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-bar);
  padding: 10px 12px;
  margin-bottom: 18px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.rule-label {
  color: var(--text-muted);
  font-size: 11px;
  letter-spacing: 1px;
}

.rule-select {
  flex: 1;
  background: transparent;
  border: none;
  color: var(--text);
  font-size: 14px;
  font-weight: 600;
  font-family: var(--sans);
  outline: none;
  padding: 4px 0;
}

.rule-select:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
  border-radius: 4px;
}

.meals {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.meal {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: var(--space-3);
}

.meal-head {
  font-family: var(--sans);
  font-size: 10px;
  letter-spacing: 2px;
  color: var(--accent);
  text-transform: uppercase;
  margin: 0 0 8px;
  display: flex;
  align-items: center;
  gap: 6px;
}

.dish {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 8px 0;
  border-top: 1px dashed var(--border-dash);
  gap: 12px;
}

.dish:first-of-type { border-top: none; padding-top: 0; }

.dish-info {
  display: flex;
  align-items: baseline;
  gap: 8px;
  min-width: 0;
}

.dish-tag {
  font-size: 10px;
  color: var(--text-muted);
  font-style: italic;
  flex-shrink: 0;
}

.dish-name {
  font-family: var(--serif);
  font-size: 18px;
  font-weight: 400;
  letter-spacing: 0.5px;
  color: var(--text);
  word-break: break-word;
}

.dish-name.is-empty { color: var(--text-muted); font-style: italic; }

.swap-btn {
  background: transparent;
  border: 1px solid var(--accent);
  color: var(--accent);
  border-radius: var(--radius-pill);
  padding: 5px 12px;
  font-size: 12px;
  font-family: var(--sans);
  cursor: pointer;
  white-space: nowrap;
  transition: background 0.15s, color 0.15s;
}

.swap-btn:hover, .swap-btn:focus-visible {
  background: var(--accent);
  color: var(--text-cta);
  outline: none;
}

.swap-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.actions {
  display: flex;
  gap: 10px;
  margin-top: 14px;
}

.btn {
  flex: 1;
  border: none;
  border-radius: var(--radius-pill);
  padding: 13px;
  font-size: 13px;
  font-family: var(--sans);
  letter-spacing: 2px;
  cursor: pointer;
  transition: transform 0.1s, opacity 0.15s;
}

.btn:active { transform: scale(0.97); }

.btn-primary {
  background: var(--text);
  color: var(--text-cta);
}

.btn-secondary {
  background: rgba(255, 255, 255, 0.7);
  color: var(--accent);
  border: 1px solid var(--accent);
}

.btn-primary:hover { opacity: 0.9; }
.btn-secondary:hover { background: var(--accent); color: var(--text-cta); }

.tip {
  font-family: var(--serif);
  font-size: 10px;
  color: var(--text-muted);
  font-style: italic;
  text-align: center;
  margin: 14px 0 0;
}

.error {
  margin-top: 20px;
  padding: 14px;
  background: #fee2e2;
  border: 1px solid #fca5a5;
  border-radius: var(--radius-card);
  color: #991b1b;
  font-size: 14px;
  text-align: center;
}

.error[hidden] { display: none; }

@keyframes swap-flash {
  0%   { background: rgba(180, 83, 9, 0); }
  20%  { background: rgba(180, 83, 9, 0.18); }
  100% { background: rgba(180, 83, 9, 0); }
}

.dish.is-swapping .dish-name { animation: swap-flash 0.4s ease-out; }

@media (min-width: 480px) {
  #app { padding: 28px 24px 40px; }
  .hero-title { font-size: 36px; }
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/suunto/recipe-app
git add style.css
git commit -m "feat(ui): warm editorial style.css"
```

---

## Task 12: Build `app.js` — load data + initial render

**Files:** Create `app.js`

- [ ] **Step 1: Write `app.js` (full file)**

Write to `/Users/suunto/recipe-app/app.js`:

```js
// app.js — DOM wiring for Meal Wheel.

import { noRepeat, pureRandom, flavorRotation, mainRepeat, swapDish, SLOTS } from './engine.js';
import { formatCopyText } from './formatter.js';

const RULES = {
  noRepeat:      { label: '一天不重复',      run: (cats) => noRepeat(cats) },
  pureRandom:    { label: '纯随机',          run: (cats) => pureRandom(cats) },
  flavorRotation:{ label: '风味轮换',        run: (cats) => flavorRotation(cats) },
  mainRepeat:    { label: '主菜重复副菜换',  run: (cats) => mainRepeat(cats) },
};

const MEAL_META = {
  breakfast: { label: '早餐', emoji: '🌅', en: 'Breakfast' },
  lunch:     { label: '午餐', emoji: '☀️', en: 'Lunch'     },
  dinner:    { label: '晚餐', emoji: '🌙', en: 'Dinner'    },
};

const state = {
  categories: null,
  plan: [],
  rule: 'noRepeat',
};

const els = {
  heroDate:  document.getElementById('hero-date'),
  ruleSelect:document.getElementById('rule-select'),
  meals:     document.getElementById('meals'),
  copyBtn:   document.getElementById('copy-btn'),
  rerollBtn: document.getElementById('reroll-btn'),
  error:     document.getElementById('error'),
};

// --- Data loading -----------------------------------------------------------

async function loadData() {
  try {
    const res = await fetch('data/dishes.json');
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    const data = await res.json();
    state.categories = data.categories;
  } catch (err) {
    showError('数据加载失败 — 请检查网络后刷新');
    throw err;
  }
}

function showError(msg) {
  els.error.textContent = msg;
  els.error.hidden = false;
}

// --- Generation -------------------------------------------------------------

function generate() {
  if (!state.categories) return;
  state.plan = RULES[state.rule].run(state.categories);
  render();
}

// --- Rendering --------------------------------------------------------------

function formatHeroDate(d) {
  const days = ['SUN', 'MON', 'TUE', 'WED', 'THU', 'FRI', 'SAT'];
  const m = d.getMonth() + 1;
  const day = d.getDate();
  return `${d.getFullYear()} · ${m < 10 ? '0' + m : m} · ${day < 10 ? '0' + day : day} · ${days[d.getDay()]}`;
}

function render() {
  els.heroDate.textContent = formatHeroDate(new Date());
  // Group slots by meal.
  const byMeal = { breakfast: [], lunch: [], dinner: [] };
  for (const entry of state.plan) {
    const meal = entry.slotId.split('-')[0];
    byMeal[meal].push(entry);
  }
  // Each meal: protein first, then carb.
  for (const meal of Object.keys(byMeal)) {
    byMeal[meal].sort((a, b) => a.slotId.endsWith('protein') ? -1 : 1);
  }

  els.meals.innerHTML = '';
  for (const meal of ['breakfast', 'lunch', 'dinner']) {
    const meta = MEAL_META[meal];
    const section = document.createElement('section');
    section.className = 'meal';
    section.setAttribute('aria-labelledby', `${meal}-head`);

    const head = document.createElement('h2');
    head.className = 'meal-head';
    head.id = `${meal}-head`;
    head.textContent = `${meta.emoji} ${meta.label} · ${meta.en}`;
    section.appendChild(head);

    for (const entry of byMeal[meal]) {
      const row = document.createElement('div');
      row.className = 'dish';
      row.dataset.slotId = entry.slotId;

      const info = document.createElement('div');
      info.className = 'dish-info';

      const tag = document.createElement('span');
      tag.className = 'dish-tag';
      tag.textContent = entry.slotId.endsWith('protein') ? '蛋白' : '碳水';

      const name = document.createElement('span');
      name.className = 'dish-name';
      name.textContent = entry.dish;

      info.appendChild(tag);
      info.appendChild(name);

      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = 'swap-btn';
      btn.textContent = '换一道';
      btn.setAttribute('aria-label', `换掉 ${entry.dish}`);
      btn.addEventListener('click', () => handleSwap(entry.slotId, row));

      row.appendChild(info);
      row.appendChild(btn);
      section.appendChild(row);
    }

    els.meals.appendChild(section);
  }
}

function renderSwapFlash(row) {
  row.classList.remove('is-swapping');
  // Force reflow so the animation restarts.
  void row.offsetWidth;
  row.classList.add('is-swapping');
}

// --- Handlers ---------------------------------------------------------------

function handleSwap(slotId, row) {
  const used = state.plan.map(p => p.dish);
  const newEntry = swapDish(state.plan, slotId, state.categories, used);
  const idx = state.plan.findIndex(p => p.slotId === slotId);
  state.plan[idx] = newEntry;
  // Update only the affected row's text + flash.
  const nameEl = row.querySelector('.dish-name');
  nameEl.textContent = newEntry.dish;
  row.querySelector('.swap-btn').setAttribute('aria-label', `换掉 ${newEntry.dish}`);
  renderSwapFlash(row);
}

function handleRuleChange() {
  state.rule = els.ruleSelect.value;
  generate();
}

function handleReroll() {
  generate();
}

async function handleCopy() {
  const text = formatCopyText(state.plan, new Date());
  try {
    if (navigator.clipboard && window.isSecureContext) {
      await navigator.clipboard.writeText(text);
    } else {
      // Fallback for older browsers / non-secure contexts.
      const ta = document.createElement('textarea');
      ta.value = text;
      ta.style.position = 'fixed';
      ta.style.opacity = '0';
      document.body.appendChild(ta);
      ta.select();
      document.execCommand('copy');
      document.body.removeChild(ta);
    }
    flashCopyBtn('已复制 ✓');
  } catch {
    flashCopyBtn('复制失败');
  }
}

function flashCopyBtn(msg) {
  const original = els.copyBtn.textContent;
  els.copyBtn.textContent = msg;
  setTimeout(() => { els.copyBtn.textContent = original; }, 1200);
}

// --- Service worker registration -------------------------------------------

if ('serviceWorker' in navigator) {
  window.addEventListener('load', () => {
    navigator.serviceWorker.register('sw.js').catch(() => { /* silent */ });
  });
}

// --- Init -------------------------------------------------------------------

(async function init() {
  await loadData();
  generate();
  els.ruleSelect.addEventListener('change', handleRuleChange);
  els.rerollBtn.addEventListener('click', handleReroll);
  els.copyBtn.addEventListener('click', handleCopy);
})();
```

- [ ] **Step 2: Commit**

```bash
cd /Users/suunto/recipe-app
git add app.js
git commit -m "feat(ui): app.js wires data load, render, swap, reroll, copy"
```

---

## Task 13: Manual smoke test in browser

**Files:** (none — this is a manual verification step)

- [ ] **Step 1: Start a local web server**

Run: `cd /Users/suunto/recipe-app && python3 -m http.server 8000 &`
Expected: server starts, logs `Serving HTTP on :: port 8000`.

- [ ] **Step 2: Open the app in a browser**

Open: `http://localhost:8000`
Expected: page loads with the date, title "温润的餐桌", a rule dropdown, 3 meal cards with 2 dishes each, and 2 bottom buttons.

- [ ] **Step 3: Test "重新配餐"**

Click: "🎲 重新配餐"
Expected: 6 dishes change. (With ~1400 dishes, all 6 are very likely to change.)

- [ ] **Step 4: Test "换一道" on a dish**

Click: "换一道" next to the breakfast protein dish.
Expected: only that one dish's name changes. Brief flash animation.

- [ ] **Step 5: Test rule switching**

Change: rule dropdown to "纯随机"
Expected: all 6 dishes reroll.

Change: rule dropdown to "风味轮换"
Expected: reroll. Verify breakfast + lunch share one flavor; dinner is the other.

Change: rule dropdown to "主菜重复副菜换"
Expected: reroll. Verify lunch protein == dinner protein.

- [ ] **Step 6: Test "复制今日"**

Click: "📋 复制今日"
Expected: button briefly shows "已复制 ✓". Paste into a text editor — content matches the format spec (3 meals × protein/carb with Chinese labels).

- [ ] **Step 7: Verify responsive layout**

In browser devtools, switch to mobile mode (iPhone 12, 390×844).
Expected: layout fills width, no horizontal scroll, touch targets ≥ 44px.

- [ ] **Step 8: Stop the local server**

Run: `lsof -ti:8000 | xargs kill -9 2>/dev/null; true`
Expected: server stops.

- [ ] **Step 9: Commit (no changes; this is a verification task)**

```bash
cd /Users/suunto/recipe-app
git status
# (expect: clean working tree)
```

---

## Task 14: PWA — manifest, icons, service worker

**Files:** Create `manifest.webmanifest`, `sw.js`, `icons/icon-192.png`, `icons/icon-512.png`

- [ ] **Step 1: Create `icons/` directory**

Run: `mkdir -p /Users/suunto/recipe-app/icons`

- [ ] **Step 2: Generate the two PWA icons from a simple SVG**

Create the source SVG by writing to `/Users/suunto/recipe-app/icons/icon.svg`:

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <rect width="512" height="512" rx="96" fill="#fdf6e3"/>
  <text x="256" y="320" text-anchor="middle" font-family="Georgia, serif"
        font-size="280" font-style="italic" fill="#b45309">温</text>
</svg>
```

- [ ] **Step 3: Convert the SVG into 192 and 512 PNGs**

Run (requires `rsvg-convert` or `qlmanage`; if neither available, see fallback below):

```bash
# Try rsvg-convert first
if command -v rsvg-convert >/dev/null; then
  rsvg-convert -w 192 -h 192 icons/icon.svg -o icons/icon-192.png
  rsvg-convert -w 512 -h 512 icons/icon.svg -o icons/icon-512.png
elif command -v qlmanage >/dev/null; then
  # qlmanage can render to PNG on macOS
  qlmanage -t -s 192 -o /tmp icons/icon.svg && mv /tmp/icons.icon.png icons/icon-192.png
  qlmanage -t -s 512 -o /tmp icons/icon.svg && mv /tmp/icons.icon.png icons/icon-512.png
else
  echo "No SVG-to-PNG tool found. Please install librsvg (brew install librsvg) and re-run this step."
  exit 1
fi
```

Expected: `icons/icon-192.png` and `icons/icon-512.png` exist, each > 0 bytes.

- [ ] **Step 4: Write `manifest.webmanifest`**

Write to `/Users/suunto/recipe-app/manifest.webmanifest`:

```json
{
  "name": "温润的餐桌",
  "short_name": "餐桌",
  "description": "为今天配的六道菜",
  "start_url": ".",
  "display": "standalone",
  "background_color": "#fdf6e3",
  "theme_color": "#b45309",
  "lang": "zh-CN",
  "icons": [
    { "src": "icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

- [ ] **Step 5: Write `sw.js`**

Write to `/Users/suunto/recipe-app/sw.js`:

```js
// sw.js — cache-first service worker for Meal Wheel.
const CACHE_NAME = 'meal-wheel-v1';
const ASSETS = [
  './',
  'index.html',
  'app.js',
  'engine.js',
  'formatter.js',
  'style.css',
  'manifest.webmanifest',
  'data/dishes.json',
  'icons/icon-192.png',
  'icons/icon-512.png',
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(ASSETS))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((keys) =>
      Promise.all(keys.filter((k) => k !== CACHE_NAME).map((k) => caches.delete(k)))
    )
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const { request } = event;
  if (request.method !== 'GET') return;
  event.respondWith(
    caches.match(request).then((cached) => {
      if (cached) return cached;
      return fetch(request).then((response) => {
        // Optionally cache successful same-origin responses for offline use.
        if (response.ok && new URL(request.url).origin === self.location.origin) {
          const clone = response.clone();
          caches.open(CACHE_NAME).then((cache) => cache.put(request, clone));
        }
        return response;
      });
    })
  );
});
```

- [ ] **Step 6: Commit**

```bash
cd /Users/suunto/recipe-app
git add icons/ manifest.webmanifest sw.js
git commit -m "feat(pwa): manifest, icons, service worker"
```

---

## Task 15: Write `README.md`

**Files:** Create `README.md`

- [ ] **Step 1: Write `README.md`**

Write to `/Users/suunto/recipe-app/README.md`:

```markdown
# Meal Wheel · 温润的餐桌

A tiny static web app that picks 6 dishes a day (3 meals × protein + carb) from a 1400-dish Chinese/Western library. The system picks; you only accept or swap individual dishes.

## Stack

- Pure HTML + CSS + vanilla JS (ES modules)
- No build step
- Vitest for unit tests (algorithm only)
- PWA-enabled: install to home screen on iOS / Android
- Hosted on GitHub Pages

## Local development

```bash
# 1. Install test deps
npm install

# 2. Run tests
npm test

# 3. Serve the app locally
python3 -m http.server 8000
# Open http://localhost:8000
```

## Updating the recipe library

The recipe library lives outside this repo at `~/Documents/饮食菜品库/`. After editing those `.md` files:

```bash
python3 scripts/sync_dishes.py    # rebuilds data/dishes.json
git add data/dishes.json
git commit -m "data: <what changed>"
git push                            # auto-deploys to GitHub Pages in ~10s
```

## Deployment

The app is deployed to **https://suunto.github.io/meal-wheel/** via GitHub Pages.

To set up from scratch on a new machine:

1. `git clone` this repo
2. `npm install`
3. `python3 scripts/sync_dishes.py` (regenerates the dish JSON from your local library)
4. Edit the GitHub Pages source in repo Settings → Pages → `main` branch, root.
5. Push to `main`; GitHub deploys automatically.

## File layout

```
index.html              single page
app.js                  DOM wiring
engine.js               generation algorithms (pure, unit-tested)
formatter.js            clipboard text formatter (pure, unit-tested)
style.css               warm editorial styles
data/dishes.json        generated by sync_dishes.py
scripts/sync_dishes.py  .md → JSON converter
icons/                  PWA icons
manifest.webmanifest    PWA manifest
sw.js                   service worker
tests/                  Vitest unit tests
```

## License

Personal project. All rights reserved by the author.
```

- [ ] **Step 2: Commit**

```bash
cd /Users/suunto/recipe-app
git add README.md
git commit -m "docs: README with setup, sync, deploy instructions"
```

---

## Task 16: Push to GitHub and enable Pages

**Files:** (none)

- [ ] **Step 1: Verify `gh` CLI is available and authenticated**

Run: `gh auth status`
Expected: "Logged in to github.com account <username>"

- [ ] **Step 2: Create the GitHub repo and push**

Run:
```bash
cd /Users/suunto/recipe-app
gh repo create meal-wheel --public --source=. --remote=origin --push
```
Expected: a new public repo at `github.com/suunto/meal-wheel`. All commits pushed to `main`.

- [ ] **Step 3: Enable GitHub Pages**

Run:
```bash
cd /Users/suunto/recipe-app
gh api -X POST repos/suunto/meal-wheel/pages -f source[branch]=main -f source[path]=/
```
Expected: HTTP 201. GitHub Pages is now serving from `main` branch root.

- [ ] **Step 4: Wait ~30 seconds, then verify the live URL**

Run:
```bash
sleep 30
curl -sI https://suunto.github.io/meal-wheel/ | head -3
```
Expected: `HTTP/2 200` (or `HTTP/1.1 200 OK`).

- [ ] **Step 5: Visit the URL in a browser, smoke-test live app**

Open: `https://suunto.github.io/meal-wheel/`
Expected: identical behavior to the local smoke test in Task 13.

- [ ] **Step 6: Document the live URL in spec**

Open: `/Users/suunto/recipe-app/docs/superpowers/specs/2026-06-24-meal-wheel-design.md`

Find the line starting with `\`https://suunto.github.io/meal-wheel/\`` (around line 238).

Verify it matches the deployed URL. (If you've changed the repo name, update it here too.)

- [ ] **Step 7: Commit any spec changes**

```bash
cd /Users/suunto/recipe-app
git status
# If clean, skip. Otherwise:
git add docs/
git commit -m "docs: confirm live URL"
```

---

## Self-Review Notes (writer's checklist, not a task)

- **Spec coverage:** Every section in the spec maps to at least one task.
  - §1 Purpose: covered by overall plan goal
  - §2 Core Decisions: covered by Tasks 3–9 (engine), 10–12 (UI), 14 (PWA)
  - §3 Generation Rules: Tasks 4–7 (one per rule) + Task 8 (swap)
  - §4 Architecture: Tasks 1, 2 (data sync), 10–12 (UI structure)
  - §5 UI Spec: Tasks 10 (HTML), 11 (CSS), 12 (JS wiring)
  - §6 PWA: Task 14
  - §7 Deployment: Task 16
  - §8 sync_dishes.py: Task 2
  - §9 Testing: Tasks 3–9 (TDD per algorithm)
  - §10 Error Handling: Task 12 (try/catch around fetch, disabled swap, fallback clipboard)
  - §11 Out of Scope: not implemented, by design
  - §12 Open Questions: none
  - §13 Glossary: not needed in code, referenced implicitly
  - Implementation Sequence (preview): 1–10 in spec maps to 1–16 here (spec grouped some together)

- **Placeholder scan:** No TBD / TODO / "fill in later" / "add appropriate handling" found.

- **Type / name consistency:** `SLOTS`, `categoryKeyFor`, `noRepeat`, `pureRandom`, `flavorRotation`, `mainRepeat`, `swapDish`, `formatCopyText` are defined and used consistently across tasks. The slot id format (`breakfast-protein` etc.) is identical throughout.
